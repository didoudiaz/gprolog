#!/bin/sh
do_diff() {
    diff --ignore-space-change -I '% date' -I '% compiler' -I '% time' $1 $2
}

files=*.wam


if [ $# = 2 ]; then
    do_diff $1 $2
else
    for i in $files; do
	do_diff $i ${i}1
    done
fi



